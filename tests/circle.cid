var integer i;
var integer j;
var boolean continue;

main {
  continue := true;
  i := 0;

  while (continue) {
    continue := false;
    j := 0;
    while (j < arg+1) {
      if (i*i + j*j < arg*arg) {
        print(46);
        continue := true
      } else {
        print(35)
      };
      print(32);
      j := j+1
    };
    print(10);
    i := i+1
  }
}
